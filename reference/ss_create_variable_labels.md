# Create plot labels from variable names

Create plot labels from variable names

## Usage

``` r
ss_create_variable_labels(dat)
```

## Arguments

- dat:

  Data frame of Water Quality data with variables in long format.

## Value

Returns `dat_long` with an addition column `variable_label`.
`variable_label`
