# Extract deployment dates

Extract deployment dates

## Usage

``` r
extract_deployment_dates(deployment_dates)
```

## Arguments

- deployment_dates:

  Data frame with two columns; the first column holds the deployment
  date, and the second column holds the retrieval date.
