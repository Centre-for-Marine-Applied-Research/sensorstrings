# Convert old data structure to sensorstrings format

Convert old data structure to sensorstrings format

## Usage

``` r
ss_reformat_old_data(dat)
```

## Arguments

- dat:

  Water quality data in the old format.

## Value

`dat` formatted for use with `sensorstrings` and `qaqcmar`.
