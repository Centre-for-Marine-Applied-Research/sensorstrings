# Convert column names from old log into the new format

Convert column names from old log into the new format

## Usage

``` r
ss_convert_old_log(log)
```

## Arguments

- log:

  Log file as read in from
  [`ss_read_log()`](https://dempsey-cmar.github.io/sensorstrings/reference/ss_read_log.md).

## Value

Returns log with columns names matching those as if it head been read in
from
